# Write a Python function to calculate the factorial of a number (a non-negative integer).
#  The function accepts the number as an argument

def fac():
    userinput=int(input("enter the number - "))
    fac=1
    i=0
    while i<1:
        fac=userinput*userinput
        print(fac)
        i+=1

fac()